{\rtf1\ansi\ansicpg1252\cocoartf2822
\cocoatextscaling0\cocoaplatform0{\fonttbl\f0\froman\fcharset0 Palatino-Roman;\f1\froman\fcharset0 Palatino-Italic;}
{\colortbl;\red255\green255\blue255;}
{\*\expandedcolortbl;;}
\pard\tx0\tx360\tx720\tx1080\tx1440\tx1800\tx2160\tx2880\tx3600\tx4320\sl264\slmult1\pardirnatural\partightenfactor0

\f0\fs36 \cf0 How stuff is represented in memory after tokenization, as well as runtime - how arrays and strings are represented, as well as gosub stack frames, and for loop overhead.\
\
What's kind of amusing in retrospect, is that the primary way these old, resource constrained, slow machines were programmed was an additional layer of indirection going through the BASIC interpreter. Thereby making them even slower!\
\
Tokenization was one way that the basic implementation (all 10K 
\f1\i (verify) 
\f0\i0 of it) helped compact more code into less space.  Takes the line of code, replaces keywords with single byte tokens, thereby freeing up gobs of space. "PRINT" is five bytes of characters, but is a single token.  (c.f. Inside the Apple //e)\
\
How Applesoft stores variables is a great example at packing in data.  
\f1\i (insert diagram, etc)
\f0\i0  . For example, variables have two bytes for their name (or a zero byte for a one-character name). Because ASCII only needs 7 bits, that means the high bit is available.\
\
There's four types (real, integer, string, function), and there's two bits free (high bit of the first and second character of the variable name), which gives four values.  The type of the variable is encoded by bits.  Like reals have both high bits off, functions have both high bits on.  strings and integers have one bit on and one bit off.    (c.f. Inside the Apple //e)}